import React from 'react';

export default function SearchForm(props){
    return (
        <form>
            <label>Search</label>
            <input type='search' placeholder='Pizza'/>
        </form>
    );
}